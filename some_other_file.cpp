// #include "person.h"

// Person p1{"Larry", 18};
/*
int Person::person_count = 0;

Person::Person(const std::string &name, int age)
    : name{name}, age{age}
{
    ++person_count;
}

*/

// this is a function definition, which is made available by simply including the header or the prototype
int max(int x, int y)
{
    return (x > y) ? x : y;
}